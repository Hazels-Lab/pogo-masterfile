## Important

Do NOT read or parse the following generated files:

* `./packages/typescript/index.ts`
* `./packages/rust/lib.rs`
* `./packages/go/main.go`

These files are extremely large and will waste too many tokens. When you are done with your edits, report to the user to test if the files have outputted correctly.
