{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"noEmit": true,
		"moduleResolution": "bundler"
	},
	"include": ["src/**/*"],
	// Override the root's excludes — those apply to the root context only.
	"exclude": ["node_modules", "dist"]
}
